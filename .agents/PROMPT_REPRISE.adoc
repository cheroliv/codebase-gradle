= PROMPT_REPRISE — Session 010 — Codebase Gradle
:author: cheroliv + opencode (deepseek-v4-pro)
:date: 2026-05-10
:session: 009
:prochaine: 010

[abstract]
Amorçage pour la prochaine session opencode.

== Contexte de Reprise (EAGER)

=== Ce qui a été accompli — Sessions 000→009

- Sessions 000-001 : Gouvernance agent + US-9.1 pgvector container ✅
- Sessions 002-006 : US-9.2→9.PROD (dataset, embeddings, dedup, prod bridge, metadata) ✅
- Sessions 007-008 : US-9.5/9.6 (metadata Kotlin + YAML/JSON anonymized indexing) ✅
- Session 009 : EPIC 2 MVP0 — AnonymizationExpert LangChain4j (interface + factory + fallback + Cucumber wip) ✅

EPIC 9 — 6/6 US TERMINÉES. 11/11 Cucumber PASS.
EPIC 2 MVP0 — 3 nouveaux scénarios Cucumber @wip (déterministes).

=== État du code source

[source]
----
src/main/kotlin/codebase/rag/
├── AnonymizationContext.kt     # EPIC 2 — data classes contrats
├── AnonymizationExpert.kt      # EPIC 2 — interface LangChain4j + factory + deterministic fallback
├── AnonymizationExpertMain.kt  # EPIC 2 — entry point JavaExec
├── ChunkTokenizer.kt           # EPIC 9 — ~512 token chunks
├── CodebaseIndexerMain.kt      # EPIC 9 — main() pour ./gradlew indexCodebase
├── EmbeddingPipeline.kt        # EPIC 9 — ONNX AllMiniLmL6V2
├── KotlinMetadataExtractor.kt  # EPIC 9 — package/class extraction
├── VectorStore.kt              # EPIC 9 — pgvector JDBC CRUD
└── YamlConfigAnonymizer.kt     # EPIC 9 — rule-based anonymization

Tasks codebase:
  run / indexCodebase / anonymizeWithExpert
  verifyReadMeToAnonymizedYaml / verifySiteToAnonymizedYaml
  verifySliderToAnonymizedYaml / verifyCodebaseToAnonymizedYaml
  scaffoldCodebaseYml / snapshotCodebase
----

Dépendance LangChain4j-ollama:1.12.2 intégrée dans le bundle langchain4j-rag.

== Mission du Jour — Session 010

=== Objectif prioritaire : EPIC 4 — Benchmark de perception spatiale LLM (P0)

Mesurer à quel seuil de tokens le LLM perd la notion de cercle de confiance.
Évaluer l'apport du vecteur composite (RAG pgvector + Graphify knowledge graph)
sur la résilience du contexte long via l'expert AnonymizationExpert.

Sous-tâches MVP0 :
. Créer `PerceptionBenchmark.kt` — test de dilution spatiale :
  - Injecter N tokens de contexte (0, 500, 1k, 2k, 4k, 8k, 16k, 32k)
  - Question fixe : "Quel est le cercle de confiance du fichier codebase.yml ?"
  - Mesurer accuracy par niveau de dilution
. Injecter le vecteur composite RAG (top-5 chunks pgvector) + graphify (top-3 nœuds) :
  - Comparer accuracy avec vs sans augmentation composite
. Exporter rapport benchmark en AsciiDoc
. 2-3 scénarios Cucumber (@wip)

=== Objectifs secondaires

- Nettoyer duplication SESSIONS_HISTORY (2x session 006)
- `./gradlew compileKotlin compileTestKotlin` après chaque modif
- Zéro test en fin de session sans permission

== Rappel — Règles Globales (EAGER)

. Règle 0 : publishToMavenLocal OBLIGATOIRE pour plugins
. Règle 1 : Aucun commit sans permission explicite
. Règle 1b : Backup avant modif fichier config non versionné
. Règle 2 : Pas de ./gradlew test en fin de session sans permission

[.text-right]
*PROMPT_REPRISE mis à jour le 10 mai 2026 — fin de session 009 (EPIC 2 MVP0).*
*Prochaine session : 010 — EPIC 4 Benchmark perception spatiale LLM.*
