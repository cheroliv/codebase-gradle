= Rapport Session 051 — Fix 3 échecs résiduels pgvector
:date: 2026-05-21
:author: deepseek-v4-flash-free (opencode)
:status: TERMINE
:score: 10/10

== Résumé

Fix des 3 échecs Cucumber résiduels de la session 050 (75% → 100% de résolution).

== Problèmes corrigés

[cols="1,4,4"]
|===
| # | Cause | Fix
| 1 | Locale FR → `"%.3f".format()` produit `0,297` (virgule) au lieu de `0.297` (point). Regex `\d+\.\d+` ne matche pas. | `Locale.US` dans `CompositeContextBuilder.kt:49` + `MultiChannelContextGraph.kt:113`. Regex `-?` optionnel pour signe négatif dans `CompositeContextSteps.kt:79`. Feature range étendu à `-1.0 and 1.0`.
| 2 | Header `[RÈGLES_EAGER]` non trouvé — `it.trim() == header` attend égalité exacte mais la ligne contient `[RÈGLES_EAGER] Contexte de gouvernance...` | `startsWith(header)` au lieu de `== header` dans `OpencodeInjectorSteps.kt:51`.
| 3| Query `fileType="yml"` retourne liste vide — `tokenize` filtre `.kt`+.adoc` seulement, YAML/JSON jamais indexés. | Ajout `.yml`, `.yaml`, `.json` dans `WorkspaceWalkerSteps.kt:64`. Doc count 6→9 dans `9_3_4_workspace_walker_adoc.feature:19`.
|==|

== Fichiers modifiés (6)

- `src/main/kotlin/codebase/rag/CompositeContextBuilder.kt` — import Locale + format Locale.US
- `src/main/kotlin/codebase/koog/MultiChannelContextGraph.kt` — import Locale + format Locale.US
- `src/test/kotlin/codebase/scenarios/OpencodeInjectorSteps.kt` — startsWith
- `src/test/kotlin/codebase/scenarios/CompositeContextSteps.kt` — regex `-?`
- `src/test/kotlin/codebase/scenarios/WorkspaceWalkerSteps.kt` — extension filter
- `src/test/features/9_3_4_workspace_walker_adoc.feature` — doc count 6→9
- `src/test/features/9_10_composite_context.feature` — range -1.0 and 1.0

== Résultat test

`./gradlew cucumberTest` → **BUILD SUCCESSFUL** (0 failure, 0 erreur).

== Décision

Le modèle deepseek-v4-flash-free est amplement suffisant pour ce type de bugs mécaniques.
deepseek-v4-pro (1.6TB/49 experts) serait justifié pour des EPICs de conception transverse.
