/**
 * KoogPlanningGraph supprimé — remplacé par IntentionPlanner.plan() du borough planner (Manhattan).
 * Voir planning.IntentionPlanner dans le module planner-plugin (education.cccp:planner-plugin).
 * La logique LLM de planification (classifyIntention, decomposeFlash/Pro, parsePlan) vit désormais
 * dans le plugin planner, accessible via PlannerIntegration dans codebase.
 *
 * Cette classe est obsolète depuis le cablage cross-borough codebase → planner (session 051).
 */
