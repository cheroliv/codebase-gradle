= PROMPT_REPRISE — Session 066 — EPIC V-6 Test Integration R2DBC + VibecodingGraph End-to-End
:session: 066
:date: 2026-05-27
:status: PRET
:max-lines: 50
:priorite: P0
:methodologie: BDD→TDD baby-step, 1 fichier a la fois, compiler apres chaque edition

== Mission

Terminer EPIC V-6 : test d'integration R2DBC Testcontainers dans VibecodingGraph,
verifier createSession + updateSession end-to-end avec vrai ConnectionFactory.

== Objectifs

=== P0 — Test Integration R2DBC + VibecodingGraph

- Test d'integration : VibecodingGraph avec ConnectionFactory Testcontainers
- Verifier createSession + updateSession + TokenTracker en end-to-end
- Verifier que les colonnes prompt_tokens, completion_tokens, cost, model sont persistees
- 1 scenario Cucumber minimum (ou extension @epic_v_6 existant)

=== P1 — OllamaPoolKeyAdapter (differé)

- Wrapper resilient pour OllamaProKey — pattern a definir apres test integration R2DBC
- Pattern : si Ollama renvoie quota exceeded → rotation automatique (deja dans OllamaPool)
- Article 0120 : montage docker SSH key, pas de token API

== Etat d'entree

- OllamaPool + OllamaLlmProvider fonctionnels (12 tests PASS)
- llm-pool-contracts N0 publie
- VibecodingGraph avec ConnectionFactory injectable
- SessionRepository.updateSession() persiste TokenTracker (prompt/completion/cost/model)
- V2__add_session_model.sql migration creee
- 0 regression existant

== Regles absolues

- **ZERO agent @xp ou subagent**
- **1 fichier a la fois, compile apres chaque edition**
- **Pas de cles API dans les logs**
- **Timeout 60s par appel LLM**
- **Injection par constructeur**
