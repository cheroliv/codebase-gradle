= PROMPT_REPRISE — Mission Session 037 L-1 koog PlanningGraph
:date: 2026-05-19

== Contexte

Session 036 fermée — EPIC L-0 terminé. Dépendances koog (JetBrains) ajoutées dans BOM + codebase-gradle.
Koog n'est PAS un projet à créer — c'est `ai.koog:koog-agents:0.8.0` (JetBrains, 4.2k stars, Apache 2.0).
Framework Kotlin agentique avec strategy DSL, graph-based agents, Ollama nativement supporté.
Koog cohabite avec langgraph4j (pas de suppression brutale). langchain4j préservé (LLM/RAG/pgvector).

== Décisions persistées

. *Koog = orchestrateur uniquement* — remplace `org.bsc.langgraph4j.*`, pas langchain4j.
  Le wrapper koog sur langchain4j n'est plus nécessaire : koog intègre nativement ollama.
. *Test unitaire KoogPlanningGraphTest.kt* — valide la structure du graphe (classify → flash/pro → format → END).
. *Prochaine étape L-1* : migrer `codebase.langgraph.PlanningGraph` → koog `strategy()` DSL.

== Mission session 037 (prochaine)

EPIC L-1 — Migrer PlanningGraph langgraph4j → koog strategy DSL.
Conserver ClassifyNode/ProDecomposeNode/FlashDecomposeNode/FormatNode.
Adapter les appels LLM : `OllamaChatModel` (langchain4j) → `simpleOllamaAIExecutor()` (koog).
Garder `Plan.kt` (data classes) inchangé.

== Fichiers à migrer (session 036 baseline)

- `codebase/langgraph/PlanningGraph.kt` → `codebase/koog/KoogPlanningGraph.kt` (55 lignes)
- `codebase/langgraph/PlanningState.kt` → intégré dans graphe koog
- `codebase/langgraph/ClassifyNode.kt` → nœud custom koog
- `codebase/langgraph/ProDecomposeNode.kt` → nœud custom koog
- `codebase/langgraph/FlashDecomposeNode.kt` → nœud custom koog
- `codebase/langgraph/FormatNode.kt` → nœud custom koog
- `codebase/langgraph/Plan.kt` → inchangé (move to `codebase/koog/`)

== État du workspace

- `codebase-plugin` : compile OK (langgraph4j + koog cohabitation)
- `workspace-bom` : koog 0.8.0 ajouté
- `KoogPlanningGraphTest.kt` : test structure miroir, 2 tests (build + mermaid)
