= PROMPT_REPRISE — Session 088 : EPIC OCR — OCR-3b P1 Config YAML/CLI/DSL + P2 CI/CD
:sessions: 088
:date: 2026-05-30
:prerequis: OCR-3b P0 FakeVisionProvider ✅ (session 087)
:max-lines: 35

== Quick Start

```
cd /home/cheroliv/workspace/foundry/public/codebase-gradle
./gradlew :codebase-plugin:compileKotlin
./gradlew :codebase-plugin:test --tests "codebase.ocr.*" --tests "codebase.koog.llm.*"
```

== Contexte

OCR-3b P0 terminé : VisionProvider interface + FakeVisionProvider + injection OcrTask OK.
Zéro régression. publishToMavenLocal OK. Cross-borough codex + planner OK.

== P1 — Config YAML/CLI/DSL (prioritaire)

- `llm-config.yml` → GeminiConfig → FakeVisionProvider injecté dans OcrTask
- CLI : `./gradlew ocrDocument -PinputFile=x.png` via ProjectBuilder
- DSL : `codebaseOcr { provider, model }` via gradle.properties

== P2 — CI/CD

Fichier : `.github/workflows/ocr.yml` — compile + tests OCR uniquement

== Références

- `BACKLOG.adoc § OCR-3b`
- `src/main/kotlin/codebase/ocr/CodebaseOcrExtension.kt` (DSL existant)
- `src/main/kotlin/codebase/rag/GeminiConfig.kt` (config base)
