= PROMPT_REPRISE — Mission Session 038 L-2 koog+langchain4j Contexte Augmenté
:date: 2026-05-19

== Contexte

Session 037 (L-1) fermée — orchestrateur langgraph4j → koog migré avec succès.
KoogPlanningGraph.kt : strategy{} DSL, Plan/PlanState data classes, OllamaChatModel langchain4j préservé.
L-1 commit baf6a3c push OK. 3/8 pts EPIC L complétés.

== Décisions persistées

. *Koog = orchestrateur* — remplace `org.bsc.langgraph4j.*`
. *langchain4j = scope opérationnel* — OllamaChatModel, RAG pgvector, embeddings, vector stores
. *Wrapper koog+langchain4j (L-2)* — koog injecte le contexte augmenté (CompositeContext) dans ses nœuds
. *Baby-step* : 1 variable par session. L-2 change le scope, pas l'orchestrateur.

== Mission session 038 (prochaine)

EPIC L-2 — Wrapper koog autour de langchain4j pour le contexte augmenté.
Injecter CompositeContext (RAG pgvector + embeddings + vector stores + knowledge graph)
dans les nœuds koog. Remplacer OllamaChatModel par les nœuds LLM natifs koog (nodeLLMSendMessage).
Garder Plan.kt et PlanState inchangés.

== Fichiers à modifier

- `codebase/koog/KoogPlanningGraph.kt` — remplacer OllamaChatModel → appels LLM natifs koog
- Ajouter dépendance koog prompt-executor-ollama si nécessaire
- Tests : adapter KoogPlanningGraphTest pour les nœuds LLM koog

== État du workspace

- `codebase-plugin` : compile OK, 2/2 tests koog PASS
- `workspace-bom` : koog 0.8.0
- `langgraph4j` cohabitation toujours OK (non supprimé)
- L-1 commit baf6a3c push OK
